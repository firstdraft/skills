interface:
  display_name: "Review UI consistency"
  short_description: "Review related screens and interaction states"
  default_prompt: "Use $review-ui-consistency to compare these screens and report evidenced inconsistencies without changing the app."
