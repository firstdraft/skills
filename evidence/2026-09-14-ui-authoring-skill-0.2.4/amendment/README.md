# Full collection navigation amendment

Skills candidate `702f90e5dd7a30131b8291e28b2f4b8827d0cdd9` changes one
packaged sentence from “separate paginated View all page” to “separate paginated
full collection page.” It describes the destination without prescribing a
control label. The Service amendment places overflow navigation in a final
“View more…” list row; Add placement and scoped routes remain unchanged.

The new 0.2.4 package SHA-256 is
`7c947c8837a955249a1f5cfdf0c2fbbd37b088de6293e5ef0d7e5473097bc4c7`:
134,442 bytes and 43 regular files. It contains only `create-full-stack-app` and
bundles CLI 0.2.2 at `799a184cb2453ceadf5575f7b46ba975e084f192`.
`package-diff.json` proves the inventory is unchanged and shows the entire
one-sentence packaged diff. The schema remains byte-identical to Service 3ab:
`5494a81d41d78bdedabfa58602c520da252201d0ec6fdf314be5eb6d4685805a`.
The amended Service candidate is `790455f4a4e84a3898e6194dddc5cb7ab5d481e9`.
Its changed-path set excludes schema/API/Core inputs; the operator owns the
final merged and deployed identities.

All 121 repository tests passed. Deterministic packing against the exact CLI
passed under normal and restrictive umask. Both isolated adapters passed against
the new extracted tarball: Claude 2.1.267 and Codex 0.154.0 each discovered the sole
Skill and exercised bundled CLI 0.2.2 local key generation without credentials
or a global CLI. Logs are in `checks/`.

## Reuse scope

The earlier six Appearance, private-native, and Android-provider cases retain
exactly their original prompts, fixtures, responses, source/package identities,
and limitations in the adjacent `evidence/` packet. They ran against package
`90df9cd3cf21d15f39c667106be997c29358ddc8a58c20eb3159896ef9344407`.
They are reused observations, not executions against the new digest. The change
to one collection-navigation noun phrase does not affect those cases' questions,
fixtures, or governing Appearance, authentication/native, and provider-limit
sections. No claim is made that the entire package input is unchanged.

Only `ui-foundation-read-back` is rerun in both clients against the new package;
its existing prompt already asks about navigation without prescribing the old
label. New responses and tool traces belong to this amendment packet. Original
responses are preserved in the earlier packet rather than overwritten.

This packet adds no hosted installation, Codespace, authentication, Compilation,
native runtime, publication, catalog, or deployment proof. No deferred UI Skill
was edited, selected, packaged, or evaluated. Source changes and release actions
remain separate; this workstream makes no external release mutation.

## Rerun observations

Both reruns completed successfully and preserved all inputs: Claude in 45.26
seconds and Codex in 43.35 seconds. Both correctly describe the full collection
destination, Add/scoped form, scoped return, enum I18n, stock Zinc web tokens,
native-only colors, and light default without the obsolete View all label.

Claude explicitly explains POST, nested parameters, and the absence of a hidden
parent, but again adds assumptions about omitted Plan details and describes the
absence of Credit show without the selected-association fallback. Codex calls
the entry View more and gives a concise, correct core account, but omits explicit
POST/nested-parameter and flat-member-route details. The reference still gives
all those details. These are bounded advisory results, not literal all-criteria
passes or validated complete Plans. No material defect in the one-sentence
amendment was identified, and no extra source changes were made.

## Integrated source

The release operator merged the amendment as
`66eeb1ab330646e6d998ba1448c26c8b366ef806`. An independent local `git diff`
between that commit and qualified source `702f90e5` is empty; both have tree
`da8e7e7632447fc9aa423ab249082644c2fe5a10`. The same package and local
qualification therefore apply to the integrated Skills source.

Claude used Skill/Read/Glob/Grep. Codex used five read-only shell commands and
one web lookup of the exact Service profile URL. This was not an offline-only
run. Neither client ran a First Draft API, Revyl, authentication, installation,
Compile, or product mutation command.
