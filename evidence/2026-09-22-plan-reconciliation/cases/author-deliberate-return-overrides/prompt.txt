For this public movie-rating app, keep standalone Rating creation returning to the Ratings list as already modeled. After editing a Rating, open its Movie details. When adding a Rating from a Movie’s ratings collection, successful Save should also open the Movie details; Cancel should still go back to that scoped collection. Other navigation uses the ordinary defaults. Preserve the model, empty initial data and no Account choice. Update the local First Draft Plan and explain the exceptions; do not submit or Compile.
