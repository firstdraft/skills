# Useful first preview and artifact discovery

This source candidate implements the guidance in [Skills #83](https://github.com/firstdraft/skills/issues/83) and
[#84](https://github.com/firstdraft/skills/issues/84). The
[machine receipt](2026-09-22-first-preview.json) owns exact case outputs and source/package identities. This is local
source and application evidence; it does not claim installed-client discovery, publication, deployment, or colleague
trial readiness.

## Fresh-context observations

Seven isolated Codex subagents received only the source Skill, one prompt and the declared fictional fixtures.
They did not receive the rubric or sibling conversations. The collaboration tool did not attest the exact model.
Original prompts and responses are retained under [the cases directory](2026-09-22-first-preview/cases/), together
with six authored Plans and applicable notes. Response files retain their original local links; the receipt maps
them to committed artifacts. Private CLI state and tokens are excluded.

| Case | Observed behavior |
|---|---|
| Movie proposal | Proposed three movies, two disposable viewers and two connected bookmarks without an explicit sample-data request; retained subject identities and added the requested watched edit. |
| Explicit empty start | Preserved the exact input Plan and added neither data nor credentials. |
| Account-free habits | Proposed twelve connected records with active, paused and empty examples, without inventing authentication. |
| Artifact invitation | Asked once within three consequential opening questions; made examples optional and offered synthetic preview data. |
| Model-only CSV | Separated Equipment, Member and Loan, identified consequential ambiguity, and copied no source cell values into Plan or notes. |
| Authorized synthetic reuse | Kept the original outside a separate planning root and proposed seven invented records preserving useful relationship/state variety. |
| Return form | Modeled item-level returns and unlimited photos with captions, retained the structured image Field and disclosed its service-support gap. |

All six Plans subsequently passed the bundled Plan 0.20 JSON Schema in the parent evaluator. Most agents correctly
reported that their isolated local environment lacked a validator. The global CLI available during those cases was
0.3.0; they used only local initialization/UUID operations and did not claim compatibility with remote 0.4.0 work.
The form fixture is a readable document; this does not qualify PDF or image extraction. A structured image Field is
skipped at import as a service-support gap, separate from outside-grammar upload/interface requirements in notes.

Two additional fresh resumption checks exercised pre-commit amendment drafts, identified separately in the receipt.
The explicit-root check used an earlier paragraph without the later separate-output exception; the delegated-output
check used the Skill bytes subsequently committed at `cdddec1`. An explicit current-folder request with
unresolved model-only originals prompted a focused destination question while retaining prior approval. When
the output directory was delegated, the agent chose the absent `./application` directory and left the original
untouched without another question. Neither check performed filesystem moves or Compilation.

## Actual local follow-through

The parent evaluator used exact CLI 0.4.0 source `660c02e46cdf36ec76dd556de8c96ef67ed3b035` with a private local
Service at base `03163b80e2fac0ac820a18ba1bde9a4427413e9d` plus the README change retained in
[Service #770](https://github.com/firstdraft/firstdraft/pull/770), head
`ece840d74cadf1db47ace677d150250ad76f6606`. It submitted the unchanged movie, empty, habit and derived-loan Plans,
reviewed their results, compiled into their current folders, and followed generated setup instructions. The
[Service receipt](https://github.com/firstdraft/firstdraft/blob/ece840d74cadf1db47ace677d150250ad76f6606/docs/solutions/2026-09-22-first-preview.json)
retains submitted bytes, GapSets, emitted README/seed files, runtime source snapshots and observed behavior. This
README companion was unmerged at observation; it is not a claim about deployed or distributed Service output.

- Movies: three visible titles, both documented initial logins, linked watched/unwatched bookmarks, a successful
  watched update, and an empty second viewer. The three reviewed gaps concern a boolean default and two deletion
  outcomes; none drops the sample records.
- Explicit empty: no sample records or README credentials, and a visible empty movie index. The authored candidate
  has no Movie/Bookmark create flow, so no such flow is claimed.
- Account-free habits: three goals, four active/paused habits and five dated logs with their relationships; no login.
- Derived loans: two equipment items, two borrowers and three loans, including a returned loan and two outstanding
  loans. Borrower detail preserved related history. Source CSV/cell values were absent from the generated seed,
  README, submitted provenance and design archive.

The latter two Plans had no gaps. Separate private database controls observed zero demo records under production
configuration and an unchanged seven-record set after reseeding, including a preserved changed password.
An edited ordinary lookup tuple can be recreated on reseeding: changing watched from false to true and then
reloading seeds produced the original false tuple alongside it. That is the existing
[#744 behavior](https://github.com/firstdraft/firstdraft/pull/744), not identity preservation across edits. The final
recovery guidance now explains it; no seed mechanism changed.

## Review and checks

The initial independent review used Opus 5.5 (`claude-opus-5-5`) in a clean detached checkout. Its reproducible
package-digest finding was repaired, its form-rubric classification was corrected, and its artifact-handling finding
moved the check into pre-adoption instructions. The final amendment also says to preserve an already archived
original and resolve its destination before committing, using already agreed handling when available.

Local repository checks, real-CLI deterministic packaging, hosted checks and the review amendment are reported
against their tested heads in the PR. The quick Skill validator passed. The earlier 123-test suite and both hosted
Node jobs passed at `6eb11be`; later amendment checks remain distinct. The package remains an unpublished 0.4.0
source candidate. No package version, CLI/service identity, compatibility range or catalog selection changed.
