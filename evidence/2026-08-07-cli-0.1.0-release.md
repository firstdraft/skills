# CLI 0.1.0 release — 2026-08-07

The protected CLI [release workflow](https://github.com/firstdraft/cli/actions/runs/31214599182) completed successfully
for exact tag and first-parent `main` commit `d37d8b6775a0b97ce10bd651485bd308fed1dda2`. Read-only registry reconciliation
then observed:

- `@firstdraft.com/cli@0.1.0` exists in the public npm registry;
- npm `next` resolves to `0.1.0`;
- npm `latest` still resolves to historical `0.1.0-alpha.2`;
- a fresh exact-version installation reports `firstdraft --version` as `0.1.0`; and
- `npm audit signatures` verifies the registry signature and provenance attestation.

This establishes the exact CLI package release only. It does not publish the Claude plugin, promote the plugin
catalog, activate service API contract 0.2, deploy First Draft, authenticate an agent, or exercise staging or GitHub
Publication. No Skills package, catalog, service, or deployment mutation was performed while recording this evidence.
