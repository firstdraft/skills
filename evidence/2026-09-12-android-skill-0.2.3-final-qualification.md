# Reviewed Android Skill 0.2.3 qualification

This record qualifies the revised unpublished package after the
[earlier candidate](2026-09-12-android-skill-0.2.3-qualification.md). It does not record npm publication, catalog
promotion, service deployment, or a fresh student journey. The
[machine receipt](2026-09-12-android-skill-0.2.3-final-qualification.json) binds the current package, inputs, responses,
and transcripts. The earlier receipt's digest and observations remain unchanged.

## Exact tuple

| Input | Identity |
|---|---|
| Plugin | `@firstdraft.com/claude-code@0.2.3` |
| Tarball SHA-256 | `0c882463c407f95f472527f6ff3f378edafe872a68121d78d0d9ca872faa11cc` |
| CLI | `0.2.2`, source `799a184cb2453ceadf5575f7b46ba975e084f192` |
| Service integration | `fe11462d42ee1848fe7dde0ec0dbc75fa040ee32` |
| Packaged service reference | `89a2d6866f9448f4e75b58cac26f61c52daaa0b0` |
| Android Core | `77a5e65714a9d3c1d3cc26747ab5d772d37d221d` |
| iOS Core | `24cdbca30243c9845420c6772b2d1f0f9b69e487` |
| iOS Core archive SHA-256 | `fb9ad88dfe86f8f2bc1490f9059c1ff5d400430385c7da4e80308b91f92ea788` |
| API / Plan | `0.3.0` / `firstdraft.foundation-plan.sketch/0.19` |

The reference-to-integration delta remains two generated-runtime test assertions. The service's
[Android receipt](https://github.com/firstdraft/firstdraft/blob/fe11462d42ee1848fe7dde0ec0dbc75fa040ee32/docs/solutions/2026-09-12-generated-android-preview.md)
owns Compilation, GitHub APK delivery, local public forms/navigation/theme, and the Revyl WebView limitation.
The [iPhone receipt](https://github.com/firstdraft/firstdraft/blob/fe11462d42ee1848fe7dde0ec0dbc75fa040ee32/docs/solutions/2026-09-12-revyl-preview-release.md)
separately owns its index/refresh browser observation; it does not claim Revyl forms or Back. Generated iPhone form
routing is described as source behavior, without borrowing Android runtime proof. These service links require
repository access.

## Corrections and checks

All packaged enum descriptions now agree with the emitted model: Rails `enum`, inclusion and presence validation,
and disabled scopes and instance methods. The source's explicit options explain why the macro does not imply
predicate/bang helpers. The enum presence correction passed the Oscar, Case Chat, and Photogram generated-runtime
checks through both migration and fresh-schema paths; the integrated service's
[full CI](https://github.com/firstdraft/firstdraft/actions/runs/34702199301) passed. Those are Compiler checks, separate
from the advisory model cases here.

The native reference now explains optional domains, provisional identifiers, platform identity limits, and the
omitted-client gap. Appearance's stock-launcher-icon gap is conditional on authored Appearance. Version coupling is
checked against structured compatibility metadata. `RELEASING.md` owns the selected release gates; the eval index
routes there instead of imposing a conflicting unconditional replay of the 0.2.2 service smokes.

The 95 automated repository tests, exact-CLI deterministic package check, Claude strict validation, and Codex
0.154.0 isolated installation passed. Packing with ordinary and restrictive `077` umasks produced the same digest;
the two generated portable manifests retain canonical `0644` modes. The installed adapter invoked bundled CLI
0.2.2 and local generation without a global CLI or credentials. No package-only check called First Draft.

## Four cases against the revised bytes

Fresh workspaces staged the current case fixtures and this exact unpacked package. Claude Code 2.1.267 used
`claude-opus-5[1m]` with Read, Glob, Grep, and Skill tools. Codex 0.154.0 used `gpt-6-astra` in an ephemeral read-only
run. Both clients passed `private-native-request-preserves-current-boundary` and
`android-preview-respects-provider-limit`.

Both private reviews retained iPhone and Android requests, all requested CRUD surfaces, and staff-only intent.
They explained that a public index is required to emit native clients and refused to make Movies public or remove
requested clients just to fit support. Every staged Plan/state byte remained unchanged; neither opened private
CLI state or invoked First Draft. Account/Policy sketches remain unvalidated proposals, not qualified private
application generation.

Both preview reviews read the guide and explained the WebView 113/120 blocker before recommending a device start,
the web/local-emulator fallback, separate GitHub build and Revyl device allowances, first private publication and
later pushes, explicit stop, and port cleanup. Both preserved the guide. Codex also read public billing pages.
Neither installed software, authenticated, created a repository, started a device, or changed a file. There is no
claim of recurring free Revyl minutes or a clean student browser preview.

These prompts explicitly selected the Skill. They do not prove spontaneous discovery, fresh sign-in, a public
catalog installation, the older Drawing Board Claude 2.1.226 model behavior, or a new hosted Compile. The Drawing
Board pin smoke and post-promotion public installs remain distinct gates.

## Approval-section reconciliation

The two approval/Compile sections are byte-identical to Skills `74f8343c5144ea3a5bcb7745a0d8b36f67e38018`, which
retains the released 0.2.2 Skill. Their SHA-256 values are:

- Read-back/approval: `73b51eeecd2db1a34de4a57c9edd383194de3a7e86031d3c3311e829ac9f2a80` (2,213 bytes).
- Selected Compile journey: `06fdb6afbe65c6041e4c9b7a03cae401a296fff8f0deab3f181d9d8d41dd6354` (1,458 bytes).

This explains the release-specific decision to test changed native advice and package adapters without repeating
the controlled-service approval smokes. It does not relabel the 0.2.2 full-journey observations as proof for these
new package bytes. Publication and promotion still require the exact-source CI, registry, and release gates.
