# API 0.4 Skills source companion

This record covers local source and package checks for the unpublished shared Claude/Codex candidate. It does not
qualify a Service deployment, installed-client journey, generated application, or release.

## Exact inputs

| Input | Identity |
| --- | --- |
| Fresh Skills `main` | `a001a24cc47b5f2c421012f6f5cb38750e14e443` |
| Initial reviewed Skills source | `fe474a3ad150d4128242f08a1ce1bdeed0631c5d` |
| Initial reviewed source tree | `2cca764fbdb97cffee93c92da4b4a377fb33ce46` |
| CLI pin amendment | `e48d73d69239ada3dd4bb884f031d2bb5b5e07bd` |
| Reviewed source after fixes | `a372e721c298c3f3bf49e91a6b62e409f9c0b16f` |
| Reviewed source tree | `884656d353542123f3a85b2116daaca9d1d93312` |
| Composed notes packet | `e9f55c643bd796dd55d1cdc85e8c056fbbd96662`, then `205d6fbb95f97a60ebed05a9b33a9758d466b10a` |
| Normalization packet | `bcde8486beea65500f6653a710814c0653a6c471` |
| Home packet | `c3b63065f835868b004c6e42d75cea707d0a7752`, then `a7f667912a3c70cf3eed46a1be6d40bfe610082b` |
| Service source inspected | `686222b9fe7c5df49c1afd129a0bd1a2ac83ce06` |
| Exact bundled schema SHA-256 | `5576ec5e10d108f0a2d0f9fa336249324642f092e4444f6c11e4ab738f3fa58b` |
| CLI source | `aca06fa57731fbc450d12e3f0eb373c469a46aa3`, tree `8fa258a54990899386e94883f482a9a2a47c87c9` |
| CLI JavaScript runtime SHA-256 | `43c74adba22419d054562c1688c088a1c78e3729e65dae608d4021641dbfdaee` |
| Shared package | `@firstdraft.com/claude-code@0.3.0`, bundling `@firstdraft.com/cli@0.3.0` |
| Packed SHA-256 | `1223fc6503cda462dcf315207128e9abf1331d75536350966ee320f3bb816c6d` |
| Local checks | Node `22.0.0` and `24.18.0`, through asdf and the configured CI matrix |
| Final packing toolchain | Node `24.18.0`, npm `11.16.0` |

The schema was copied without transformation from Service `docs/architecture/design/foundation-plan.schema.json`.
The candidate requires API `>= 0.4.0`, `< 0.5.0`, Plan `firstdraft.foundation-plan.sketch/0.20`, and
`rails-sketch/2026-09`. Current fixtures use analyzer `foundation-plan-rails/application-2026-09-19-conventions` and
compiler `foundation-plan-rails/compiler-application-2026-09-19-conventions`. These are selected source identities,
not observations of active web or worker revisions.

## Reconciliation

Current schemas, examples, references, fixtures, package inventory, and CLI checks use the selected contract.
Entity Validation errors target a Field or Reference; the removed record-wide custom target is rejected. Teaching
preserves native validation guidance, enum helper allocation, AASM initialization, ordinary Rails deletion,
explicit normalization choices, and Home selection without promising unsupported shapes.

This closes the schema and validation-support follow-ups in the earlier notes receipt: date-literal comparisons,
the bounded required-Reference comparison, enum-conditioned Reference presence/absence, and the compatible contract
for removing record-wide Validation error targets. Coordinated qualification remains separate. Retained-download
guidance now names the selected CLI's Plan/profile gate; retries and local Plan edits cannot upgrade an older
retained artifact, and replacement work still follows the user's authorized candidate and Compile workflow.

Implementation notes retain requirements outside the Plan vocabulary through the existing mode-specific handoff.
The earlier five source-context exercises remain bound to their original
[notes receipt](2026-09-19-implementation-notes-source.md). The early README task replaces the app's short product
description from retained design context and checks claims against emitted source and reviewed gaps. It names a
semantic location, not a fixed template string, and does not add a Plan field or description artifact. The generated
document companion and its emitted-app qualification remain separate; no client-discovery or feature-flag caveat
is waived.

The original Appearance Plan/analysis and resume Plan remain byte-identical to their September 14 receipt inputs.
The Case Chat Plan/GapSet retains its eval-case, repository-test, and GapSet source digest bindings. Current
evaluations use separate `appearance-current` and `resume-current` fixtures. The Case Chat case explicitly reviews
its August 28 result as history. No old result was relabeled as new Compiler proof.

## Local verification

- `npm ci --ignore-scripts` completed.
- `sh script/check` passed release metadata and deterministic shared-package checks, then **123/123** tests on each
  configured Node version with no failures or skips. Examples validate against the exact schema; the new checks
  reject a record-wide error target and preserve dated fixture bytes.
- `node script/check-cli-contract.mjs <exact-cli>` passed against the CLI above, including real initializer output
  validated by the bundled schema, bounded fake-transport journeys, and offline packed-executable checks.
- `node script/check-claude-plugin-package.mjs --cli-root <exact-cli>` passed. Repeated tarballs, including a changed
  process umask, reproduced the recorded digest, and canonical Skill bytes were shared by both client manifests.
- All **9/9** release-compatibility tests passed after the final digest update.
- `node script/check-plugin-release-order.mjs --prospective` passed after a read-only tag refresh: candidate `0.3.0`
  was newer than nine published versions, nine prior tags, and the one catalog version.
- `skill-creator/scripts/quick_validate.py` passed with PyYAML in a disposable virtual environment; no repository
  dependency was added. The entrypoint was **20,441 bytes**, below its 20 KiB limit.
- `git diff --check` passed.

The selected CLI differs from the initial reviewed `c1a8a5b5d31d91b88bd925f85c7ba734de6c5ae8` only in
`docs/commands.md` paragraph wrapping. Its runtime digest and initializer inputs are unchanged. The Skills amendment
at `e48d73d` updated only exact CLI pins and package digests; both configured source suites and actual CLI/package
checks passed again. After the documentation fixes at `a372e72`, both configured source suites and exact package
checks passed again with the final digest above. The CLI contract check's executable, fixture, and schema inputs
were unchanged by those documentation fixes.

## Review

Conversation-aware author review checked the selected contract, preserved implementation-note intent, recovery and
authorization boundaries, semantic README handoff, fixture ownership, and the distinction between source and
qualification evidence.

The independent code review found no actionable issue in executable changes, workflow pins, release metadata,
package inventory, fixture bindings, or removed assertions. It independently checked all nine analysis fixtures
through the exact CLI parser, all eleven Plan/analysis bindings and canonical GapSet digests, five unchanged
historical fixtures, and twelve input hashes declared by the September 14 qualification receipt. The isolated
review checkout remained clean at the initial reviewed source above. This did not rerun the full suite or qualify
an app.

Claude's initial independent adversarial review covered `a001a24..fe474a3` in an isolated clean checkout. The author
verified each cited finding against that source and the selected CLI/Service before applying these dispositions:

- **F-bd78ee accepted:** pinned the Field catalog authority to the selected Service source.
- **F-f11197 accepted:** documented the retained artifact's Plan/profile gate and bounded recovery.
- **F-6b5a40 accepted:** corrected the Case Chat fixture binding owner.
- **F-31f1b0 accepted with historical routing preserved:** closed the earlier source follow-ups in the evidence
  index instead of rewriting the original dated observation.
- **F-62c1dd rejected:** four proposed regex assertions only matched prose. Existing schema, initializer, fixture,
  and packaging checks remain, alongside independent source-based semantic review; no executable guarantee was
  removed. The review also prompted a concise pinned route to the known AASM 6.0.0 namespaced-scope limitation.

The initial review reported this warning verbatim:

> Warning: The checkout changed during the review: refs changed: refs/heads/codex/skills-contract-0-4.

The shared branch ref advanced for the CLI-pin amendment in the author worktree. The detached review checkout
remained clean at `fe474a3`. The bounded follow-up reviewed `fe474a3..a372e72` in the same session and **approved** the
amendment. It independently verified all four accepted fixes, the selected CLI contract, the exact final package
digest, the AASM source route, and 123/123 tests on Node 24.18.0. It retained the rejected prose-only test disposition
and reported no new material finding or checkout warning. Its one new cosmetic finding, **F-4484a0**, was verified
and fixed by restoring the digest assertion's indentation. No packaged bytes changed after the reviewed source.

Review session: `807a2657-d9ca-4331-8140-bfca218fc659`; initial job `review-mu8nlbly-55f8f4`, bounded follow-up
`review-mu8olncu-a4e5c1`. The final receipt/index addition and whitespace-only test correction preserve the reviewed
contract and package inputs. The focused documentation-route check passed **1/1**, and the release-metadata checks
passed **9/9** after those final changes.

## Remaining boundary

Read-only npm and Git checks on September 19 found plugin `next` and `latest` at `0.2.5`, CLI `next` and `latest` at
`0.2.2`, no published `0.3.0` for either package, and no plugin `claude-v0.3.0` tag. The source therefore retains its
unpublished minor version. The catalog was unchanged. These observations must be refreshed before an external
mutation.

The source checks did not contact the Service, run a database, exercise generated output, install the candidate in
either agent client, or repeat the product approval smokes. Hosted Skills CI was not run here; its selected CLI
source must be available on CLI `main`. This task performed no push, PR update, merge, tag, publication, deployment,
catalog selection, or dist-tag mutation.
The coordinator owns shared release-bundle and generated-document qualification.
