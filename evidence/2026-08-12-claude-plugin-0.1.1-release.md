# Claude plugin 0.1.1 publication — 2026-08-12

Read-only reconciliation observed exact package `@firstdraft.com/claude-code@0.1.1` after its protected-tag
publication workflow completed. The immutable source and publication identities were:

- source commit `263326a47a502b56af7780093988c6b860b2d5d2`;
- annotated tag `claude-v0.1.1`, whose tag object is `91b16537373ee567a2741783ce692cd9b2daadd3` and whose
  peeled commit is that exact source commit;
- successful GitHub Actions publication run
  [`31631531058`](https://github.com/firstdraft/skills/actions/runs/31631531058);
- package `@firstdraft.com/claude-code@0.1.1` in the public npm registry;
- registry integrity
  `sha512-imSYruwBnSgCTttXzBjHIpPQaBV7lo9T1JlthBDrngzYUM/rDw8n68lpTOFrdBxnrn2ZTzlwYk9kHc6YpbE8xw==`;
- registry SHA-1 `520772f0b1acba6ae015198ba8fd36f38bbf3f85`; and
- independently reconciled tarball SHA-256 `800e9ebd63843c7c680810979c35ade37de31d5e203e89a75a09f80d3399d656`,
  matching `release/compatibility.json` at the tagged source commit.

The registry package contains 33 files. An isolated public-registry verification reported one verified registry
signature and one verified attestation. npm `next` names `0.1.1`, while `latest` remains `0.1.0-alpha.3`. At this
pre-promotion observation, public `main` still selected plugin 0.1.0 in the Claude marketplace catalog.

This establishes only the package, tag, successful workflow, digest, provenance-presence, file count, and dist-tag
identities above. It did not install through the public marketplace commands, authenticate or call a model, configure
or call First Draft, exercise staging, Compilation, GitHub Publication, or the template-and-Codespace journey, move
`latest`, or merge the catalog promotion.
